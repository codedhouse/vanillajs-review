// Numbers

const x = 1234;
const xProto = x.__proto__;
console.log(xProto);

// Get number of digits
console.log(x.toString().length);

// Math methods
console.log(Math);

// Get random integer between 1-100
console.log(Math.floor(Math.random() * 100 + 1));
